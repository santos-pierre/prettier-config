# `@wasawp/prettier-config`

> Organization [Prettier](https://prettier.io) config.

## Usage

**Install**:

```bash
$ yarn add -D @wasawp/prettier-config

$ npm install -D @wasawp/prettier-config
```

**Edit `package.json`**:

```jsonc
{
  // ...
  "prettier": "@wasawp/prettier-config"
}
```
